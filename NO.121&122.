class Solution 
{
public:
    int maxProfit(vector<int>& prices) 
    {
      int z=0,buy=INT_MAX;
      for(int price:prices)                 //:用于增强循环
      {
        buy=min(buy,price);
        z=max(z,price -buy);
      }
      return z;
    }
};
